<?xml version="1.0" encoding="UTF-8"?>
<interface>
  <requires lib="gtk" version="4.0"/>

  <!--
    Main window layout.

    Sidebar:  branding + Close Application + version footer.
    Content:  GtkNotebook with three tabs — Dashboard, Products, Settings.
              Log panel docked below the notebook, hidden by default and
              toggled from the Settings page.
  -->
  <object class="GtkBox" id="root_container">
    <property name="orientation">horizontal</property>
    <property name="spacing">0</property>

    <!-- LEFT SIDEBAR - branding + Close -->
    <child>
      <object class="GtkBox" id="sidebar">
        <property name="orientation">vertical</property>
        <!-- 300 px reserves the sidebar's natural content width so the
             I/O backend pills ("Primary->Secondary" + ONLINE) are not
             clipped when two dashboards sit beside it in multi-station
             mode. GTK honours this as a hard minimum and shrinks the
             hexpanding dashboard panes to fit the residual width. -->
        <property name="width-request">300</property>
        <property name="css-classes">sidebar</property>

        <!-- Brand header -- logo + app title + subtitle in a single
             card at the top. The `sidebar-brand` CSS class draws the
             card chrome; the `app-logo` child is an empty box that
             carries an inline SVG via CSS background-image, so the
             logo asset is part of the stylesheet rather than a
             separate file to ship + skin per palette. -->
        <child>
          <object class="GtkBox" id="sidebar_header">
            <property name="orientation">horizontal</property>
            <property name="spacing">12</property>
            <property name="css-classes">sidebar-brand</property>

            <child>
              <object class="GtkBox" id="app_logo">
                <property name="css-classes">app-logo</property>
                <property name="valign">center</property>
              </object>
            </child>

            <child>
              <object class="GtkBox" id="brand_text_column">
                <property name="orientation">vertical</property>
                <property name="spacing">2</property>
                <property name="valign">center</property>
                <property name="hexpand">true</property>

                <child>
                  <object class="GtkLabel" id="app_title">
                    <property name="label" translatable="yes">Industrial HMI</property>
                    <property name="css-classes">sidebar-title</property>
                    <property name="xalign">0</property>
                  </object>
                </child>

                <child>
                  <object class="GtkLabel" id="app_subtitle">
                    <property name="label" translatable="yes">Production Monitor</property>
                    <property name="css-classes">sidebar-subtitle</property>
                    <property name="xalign">0</property>
                  </object>
                </child>
              </object>
            </child>
          </object>
        </child>

        <!-- USER card -- MainWindow appends the UserBadge here when
             auth is enabled. The card chrome lives on the badge
             itself (sidebar-card class on the UserBadge Box). -->
        <child>
          <object class="GtkBox" id="user_container">
            <property name="orientation">vertical</property>
          </object>
        </child>

        <!-- STATUS section -- small section label + card containing
             the SystemStatusBadge (coloured LED + state text). -->
        <child>
          <object class="GtkLabel" id="status_section_header">
            <property name="label" translatable="yes">STATUS</property>
            <property name="css-classes">sidebar-section-header</property>
            <property name="xalign">0</property>
          </object>
        </child>

        <child>
          <object class="GtkBox" id="system_status_container">
            <property name="orientation">vertical</property>
          </object>
        </child>

        <!-- AlertsPanel mount -- the panel renders its own header
             (toggleable Alerts / History + Clear all button) so we
             don't add a static section header here. vexpand on this
             container pushes Close + footer to the bottom of the
             sidebar. -->
        <child>
          <object class="GtkBox" id="alerts_container">
            <property name="orientation">vertical</property>
            <property name="vexpand">true</property>
          </object>
        </child>

        <!-- I/O section -- section label + the BackendHealthBar.
             MainWindow appends the bar only when an
             IntegrationManager was injected; the header label is
             part of the layout regardless so the bar visually
             matches the STATUS / USER sections above. -->
        <child>
          <object class="GtkLabel" id="io_section_header">
            <property name="label" translatable="yes">I/O</property>
            <property name="css-classes">sidebar-section-header</property>
            <property name="xalign">0</property>
          </object>
        </child>

        <child>
          <object class="GtkBox" id="backend_health_container">
            <property name="orientation">vertical</property>
            <property name="margin-start">12</property>
            <property name="margin-end">12</property>
            <property name="margin-bottom">8</property>
          </object>
        </child>

        <!-- Emergency Stop button. Hooked up in MainWindow to the
             DashboardPresenter's stop command so the operator can
             cut production from any tab. -->
        <child>
          <object class="GtkButton" id="estop_button">
            <property name="label" translatable="yes">E-STOP</property>
            <property name="margin-start">20</property>
            <property name="margin-end">20</property>
            <property name="margin-bottom">8</property>
            <property name="css-classes">sidebar-estop</property>
          </object>
        </child>

        <!-- Close Application button -->
        <child>
          <object class="GtkButton" id="close_app_button">
            <property name="label" translatable="yes">Close Application</property>
            <property name="margin-start">20</property>
            <property name="margin-end">20</property>
            <property name="margin-bottom">12</property>
            <property name="css-classes">sidebar-close-app</property>
          </object>
        </child>

        <!-- Live clock mount point (two labels, updated every second). -->
        <child>
          <object class="GtkBox" id="clock_container">
            <property name="orientation">vertical</property>
            <property name="margin-bottom">12</property>
          </object>
        </child>

        <!-- Footer - version info -->
        <child>
          <object class="GtkBox" id="sidebar_footer">
            <property name="orientation">vertical</property>
            <property name="spacing">4</property>
            <property name="margin-bottom">20</property>
            <property name="margin-start">20</property>
            <property name="margin-end">20</property>

            <child>
              <object class="GtkLabel" id="version_label">
                <property name="label" translatable="yes">Version 1.2.0</property>
                <property name="css-classes">sidebar-version</property>
                <property name="xalign">0</property>
              </object>
            </child>

            <child>
              <object class="GtkLabel" id="author_label">
                <property name="label" translatable="yes">Portfolio Demo</property>
                <property name="css-classes">sidebar-author</property>
                <property name="xalign">0</property>
              </object>
            </child>
          </object>
        </child>
      </object>
    </child>

    <!-- RIGHT CONTENT AREA -->
    <child>
      <object class="GtkBox" id="content_area">
        <property name="orientation">vertical</property>
        <property name="hexpand">true</property>
        <property name="vexpand">true</property>

        <!--
          Notebook is intentionally empty here. MainWindow::registerPage()
          appends each concrete Page as a tab programmatically so adding or
          rebuilding pages (e.g. for live language reload) doesn't require
          touching this .ui file.
        -->
        <child>
          <object class="GtkNotebook" id="main_notebook">
            <property name="hexpand">true</property>
            <property name="vexpand">true</property>
            <property name="tab-pos">top</property>
          </object>
        </child>

        <!-- Log Panel (hidden by default, toggled from Settings) -->
        <child>
          <object class="GtkBox" id="log_panel">
            <property name="orientation">vertical</property>
            <property name="visible">false</property>
            <property name="height-request">70</property>
            <property name="css-classes">log-panel</property>

            <child>
              <object class="GtkScrolledWindow" id="log_scroll">
                <property name="vexpand">true</property>
                <property name="hexpand">true</property>
                <child>
                  <object class="GtkTextView" id="log_text_view">
                    <property name="editable">false</property>
                    <property name="cursor-visible">false</property>
                    <property name="monospace">true</property>
                    <property name="wrap-mode">word</property>
                    <property name="css-classes">log-text</property>
                  </object>
                </child>
              </object>
            </child>
          </object>
        </child>

      </object>
    </child>
  </object>
</interface>
