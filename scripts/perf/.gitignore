# callgrind raw output (~1.7 MB per run) and stdout dump from the
# captured binary are regenerated by anyone re-running
# `capture-callgrind.sh`. The TRIMMED baseline
# `baseline.callgrind-annotate.top50.txt` is what we commit -- diff
# that file across releases to spot perf regressions.
output/
